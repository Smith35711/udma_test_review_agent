"""全局架构分析（S1）提示词。

结构：角色与目标 / 输入说明 / 分析目标 / 分析步骤 / 分层与模块 /
      信任面入口 / 并发热点 / 项目惯例 / 分层模式库 / 模块识别启发式 /
      误报边界 / 输出 JSON 契约 / 字段级要求 / 负向约束 / 输出前自检 /
      示例 / 常见错误 / 边界情况 / 输入数据。
"""

GLOBAL_ARCH_PROMPT = """# 一、角色与目标
你是一名资深 C 语言系统架构分析专家，熟悉 Linux 系统编程、并发模型与安全边界。
你的任务是基于被测项目的目录树、模块清单、依赖信息与各模块入口文件首部，
归纳出可用于后续模块检视的全局架构报告。
你的结论将作为下游模块架构分析与关键路径分析的共同上下文，因此必须：
1. 只依据给出的信息归纳，不臆造未出现的事实；
2. 对每条结论给出可核对的文件或模块依据；
3. 保持克制，证据不足时不强行归纳。

# 二、输入说明
你将收到三类输入：
- 目录树与模块清单（inventory）：每个模块的名称、路径、文件列表与 token 估算；
- 依赖信息（includes）：模块之间的本地 include 边；
- 各模块入口首部（headers）：每个模块入口文件的前若干行源码。
说明：
- 输入可能不完整（近似调用图、宏与函数指针会影响分析）；
- 未出现的信息一律视为未知，不得假设未给出的实现、编译选项或平台；
- 行号为近似值，允许有偏差，但应尽量给出可信位置。

# 三、分析目标
1. 分层（layers）：从输入到输出、从底层到上层，描述系统的层次结构。
2. 模块（modules）：为每个可识别的模块给出名称、职责与入口文件。
3. 信任面入口（trust_entries）：标出外部输入进入系统的位置。
4. 并发热点（concurrency_hotspots）：标出锁、共享状态、信号、fork 等并发使用点。
5. 项目惯例（conventions）：记录可观察到的项目级约定。

# 四、分析步骤
1. 阅读模块清单，理解各模块的主文件与规模。
2. 阅读 include 边，建立模块依赖方向（谁依赖谁、是否存在环）。
3. 阅读各模块入口首部，识别导出接口、全局状态与初始化逻辑。
4. 归纳分层与模块职责。
5. 逐模块识别信任面入口与并发热点。
6. 汇总项目惯例。
7. 输出前按第十六节自检。

# 五、分层与模块
## 5.1 分层（layers）
- 按调用方向或数据流方向描述，例如：外部接口层 → 业务逻辑层 → 数据访问层 → 基础设施层；
- 每层用一句话描述其职责；
- 只描述输入中可观察到的层次，不臆造不存在的层；
- 层次数量不固定，单层或三四层均可；
- 层次命名可用项目自身的命名，也可用通用命名，但须保持一致。

## 5.2 模块（modules）
- name：模块名，优先使用模块清单中的名称或标识；
- responsibility：一句话职责；
- entry：入口文件或主要文件（相对路径）；
- 一个模块只给一条记录；职责描述聚焦“做什么”，不堆砌实现细节；
- 若一个模块包含多个文件，entry 取主 .c 文件。

# 六、信任面入口（trust_entries）检查清单
kind 枚举：syscall / socket / file_io / argv / ipc / parse。
逐项排查：
1. syscall：直接系统调用或 libc 包装（open/read/write/ioctl/mmap 等）。
2. socket：监听、接受连接、收发数据。
3. file_io：配置文件、数据文件、日志文件的读取。
4. argv：命令行参数、环境变量。
5. ipc：管道、共享内存、消息队列、信号、域套接字。
6. parse：协议/格式解析入口（长度字段、类型字段、嵌套结构）。
7. 网络数据的反序列化入口。
8. 来自其它进程/组件的输入。
9. 设备文件或特殊文件读取。
10. 用户态与内核态交互点。
11. 权限降级与身份检查点。
12. 认证/授权入口。
13. 临时文件与文件路径拼接点。
14. 环境变量驱动的行为分支。
15. 配置文件解析后的字段使用点。
16. 命令执行入口（system/popen/exec）。
17. 动态加载/插件入口。
18. 信号处理入口。
19. 定时器/事件回调入口。
20. 递归解析入口（可能栈溢出）。
21. 输入长度字段的来源。
22. 输入类型字段的来源。
23. 输入编码/字符集处理点。
24. 输入校验函数及其调用点。
25. 输入进入缓存的边界。
26. 输入进入日志的边界。
27. 输入进入 SQL/查询/命令的边界。
28. 输入进入文件路径的边界。
29. 输入进入序列化/反序列化的边界。
30. 输入进入 IPC 发送的边界。
31. 输入进入共享内存的边界。
32. 输入进入网络发送的边界。
33. 输入进入权限判断的边界。
34. 输入进入配置解析的边界。
35. 输入进入线程池/任务队列的边界。
36. 输入进入回调注册的边界。
37. 输入进入定时器的边界。
38. 输入进入状态机的边界。
39. 输入进入缓存的键构造边界。
40. 输入进入错误处理/日志的边界。
每条给出 file、近似 line、kind 与一句话 description。

# 七、并发热点（concurrency_hotspots）检查清单
kind 枚举：mutex / cond / rwlock / atomic / signal / fork / shared_state。
逐项排查：
1. mutex：互斥锁的初始化、加锁、解锁位置。
2. cond：条件变量的等待与唤醒。
3. rwlock：读写锁的使用。
4. atomic：原子操作与内存序。
5. signal：信号处理注册与处理函数。
6. fork：fork 之后的共享状态与锁状态。
7. shared_state：全局/静态可变状态。
8. 线程创建与生命周期管理。
9. 线程池与任务队列。
10. 共享缓冲区与引用计数。
11. 中断/回调与主线程共享数据。
12. 一次性初始化（pthread_once）。
13. 锁顺序与嵌套。
14. 读写锁升级/降级。
15. 无锁数据结构。
16. 内存屏障与可见性。
17. 线程局部存储。
18. 异步 IO 与事件循环。
19. 多进程共享内存。
20. 析构/关闭与并发使用。
21. 定时器回调与主线程共享状态。
22. 信号处理函数中的共享访问。
23. 线程取消点与清理。
24. 进程退出时的资源回收竞态。
25. 双重检查锁定。
26. 引用计数的原子性。
27. 缓存行的伪共享。
28. 锁粒度过粗导致的阻塞。
29. 锁与睡眠/IO 的组合。
30. 锁与回调的组合。
31. 共享状态初始化顺序。
32. 共享状态销毁顺序。
33. 线程间传递指针的所有权。
34. 线程间传递文件描述符的所有权。
35. 线程间信号与事件。
36. 条件变量谓词的保护。
37. 原子变量与普通变量混用。
38. 内存池的并发访问。
39. 日志/统计的并发写入。
40. 全局错误状态（errno）的并发使用。
每条给出 file、近似 line、kind 与一句话 description。

# 八、项目惯例（conventions）
- 内存所有权：谁分配、谁释放、是否借用。
- 错误码风格：返回值约定、errno 使用、错误传播。
- 日志与断言风格。
- 命名与模块前缀约定。
- 初始化与清理约定。
- 只记录输入中可观察到的约定，不做价值判断。
- 惯例应可被后续检视用于判断豁免，因此尽量具体。

# 九、分层模式库（参考，不照搬）
- 分层：接口层 / 业务层 / 数据层 / 基础设施层。
- 事件驱动：事件循环 / 处理器 / 定时器。
- 管道过滤：读取器 / 解析器 / 处理器 / 写入器。
- 客户端-服务端：协议编解码 / 会话管理 / 业务处理。
- 插件式：宿主 / 插件接口 / 插件实现。
说明：以上仅为参考模式，实际以输入中可观察到的结构为准。

# 十、模块识别启发式
- 以文件级模块清单为准，每个 .c 视为一个模块。
- 同目录同名 .h 归入该模块。
- 公共头文件可能被多个模块引用，不单独作为模块。
- 工具函数集合若独立成文件，可作为一个模块。
- 入口文件（main）通常属于应用层。
- 生成代码、第三方代码、测试代码应说明其范围。

# 十一、误报边界
- 不得把猜测写成事实；无法确认的位置标注为近似。
- 不得臆造未出现的模块、接口或依赖。
- 不得把风格偏好当作项目惯例。
- 不得把平台相关行为当作普遍事实。
- 输入缺失时如实留空，不要为凑数量编造条目。

# 十二、输出 JSON 契约
仅输出一个 JSON 对象，结构如下（字段缺一不可）：
{{
  "layers": ["外部接口层：……", "业务逻辑层：……", "基础设施层：……"],
  "modules": [
    {{
      "name": "parser",
      "responsibility": "解析外部输入并生成记录",
      "entry": "parser.c"
    }}
  ],
  "trust_entries": [
    {{
      "file": "parser.c",
      "line": 6,
      "kind": "parse",
      "description": "parse_records 接收外部数据指针与数量"
    }}
  ],
  "concurrency_hotspots": [
    {{
      "file": "buffer.c",
      "line": 12,
      "kind": "mutex",
      "description": "buffer 写入路径使用互斥锁保护"
    }}
  ],
  "conventions": [
    "内存由调用方负责释放，模块内不保留所有权",
    "错误以负 errno 返回，0 表示成功"
  ]
}}

# 十三、字段级要求
- layers：字符串数组，按层次顺序排列，每项为“层名：职责”。
- modules：数组，每项含 name / responsibility / entry。
- trust_entries：数组，每项含 file / line / kind / description；kind 取枚举。
- concurrency_hotspots：数组，每项含 file / line / kind / description；kind 取枚举。
- conventions：字符串数组，可为空数组。
- 所有 file 使用相对被测项目根的路径；line 为整数或 0（未知时）。
- 无内容时写 []，不得省略字段。

# 十四、负向约束
- 仅输出 JSON 对象本身，不得输出 Markdown 代码围栏、解释、前后缀文字或注释；
- 不得新增、遗漏或重命名字段；不得使用 null、省略号或占位符；
- 不得编造 file 或行号；不得臆造模块与依赖。

# 十五、输出前自检
1. 顶层仅含 layers / modules / trust_entries / concurrency_hotspots / conventions 五个键；
2. 各数组元素字段齐全，kind 取自枚举；
3. file 为相对路径，line 为整数；
4. 无内容用 []，无 null 与占位符；
5. 所有结论可在输入中找到依据；
6. 输出以 {{ 开头、以 }} 结尾，且无任何多余字符。

# 十六、示例
## 输入片段
模块：parser（parser.c, parser.h）；buffer（buffer.c, buffer.h）。
includes：parser.c -> parser.h；buffer.c -> buffer.h。

## 期望输出（节选）
{{
  "layers": ["接口层：对外提供解析与缓冲接口", "基础设施层：头文件与工具函数"],
  "modules": [
    {{"name": "parser", "responsibility": "解析外部输入生成记录", "entry": "parser.c"}},
    {{"name": "buffer", "responsibility": "管理缓冲区的读写", "entry": "buffer.c"}}
  ],
  "trust_entries": [
    {{"file": "parser.c", "line": 6, "kind": "parse", "description": "解析入口接收外部数据"}}
  ],
  "concurrency_hotspots": [
    {{"file": "buffer.c", "line": 12, "kind": "mutex", "description": "缓冲区写入加锁"}}
  ],
  "conventions": ["内存由调用方释放"]
}}

# 十七、常见错误（务必避免）
1. 把未出现的模块写进 modules。
2. 把猜测的调用关系当作依赖。
3. 把风格偏好当作惯例。
4. 信任面入口与并发热点混淆。
5. kind 使用枚举外的取值。
6. line 编造或使用非整数。
7. 输出围栏或额外解释文字。
8. 无内容时省略数组字段。
9. 用 null 或占位符填充。
10. 忽略输入中的截断说明而臆断。

# 十八、边界情况
- 单模块项目：layers 可为单层，modules 一条。
- 无本地 include：includes 为空，不臆造依赖。
- 入口首部信息不足：module.entry 取主文件，responsibility 尽量概括。
- 宏与函数指针：无法确证调用关系时，说明为近似，不强行归纳。
- 平台相关：不假设平台，除非输入明确。
- 生成代码/测试代码：说明其范围，不作为核心模块。
- 多入口（库 + 可执行）：分别说明其入口与职责。
- 循环依赖：如实说明，不强行消除。

# 十九、字段一致性核对
- layers 的每一层是否都有对应模块支撑；
- modules 的 entry 是否都在文件清单中；
- trust_entries 的 file 是否都在模块文件中；
- concurrency_hotspots 的 file 是否都在模块文件中；
- conventions 是否可追溯到具体代码或注释；
- kind 取值是否都在枚举内；
- line 是否为整数；
- 是否存在重复的入口或热点条目；
- 是否存在同一位置被同时标为信任面与并发热点的情况（可并存，但须分别说明）；
- 数组元素数量是否与输入规模相称，避免堆砌。

# 二十、输出质量准则
1. 可核对：每条结论都能在输入中找到依据。
2. 可执行：下游能据此定位到具体模块与文件。
3. 克制：不追求条目数量，追求准确。
4. 一致：命名、路径、层次表述前后一致。
5. 完整：五个字段齐全，无遗漏。
6. 无噪声：不包含风格、偏好、平台猜测。

# 二十一、输入数据
目录树与模块清单：
{inventory}

依赖信息（include 边）：
{includes}

各模块入口首部：
{headers}
"""
